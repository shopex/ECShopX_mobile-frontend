<block>
    <block wx:if="{{!info}}">
        <view></view>
    </block>
    <block wx:else>
        <at-accordion open="{{anonymousState__temp}}" __triggerObserer="{{ _triggerObserer }}" bindonclick="funPrivateNcvrl" isAnimation="{{false}}" title="{{package_name}}" __fn_onClick="{{true}}">
            <view class="package-goods__list">
                <goods-item classes="package-goods__item" __triggerObserer="{{ _triggerObserer }}" showFav="{{false}}" showSku="{{true}}" wx:key="$original.item_id" info="{{item.$original}}" wx:for="{{loopArray0}}" wx:for-item="item" wx:for-index="_anonIdx">
                    <view class="cart-item__act" slot="checkbox">
                        <sp-checkbox wx:key="$original.item_id" __triggerObserer="{{ _triggerObserer }}" checked="{{item.$loopState__temp3}}" bindonchange="handleSelectionChange" __fn_onChange="{{true}}" data-e-onchange-so="this" data-e-onchange-a-a="{{item.$original.item_id}}"></sp-checkbox>
                    </view>
                    <view class="goods-item__sku" style="{{item.$loopState__temp5}}" bindtap="handleSkuPick" slot="spec" data-e-tap-so="this" data-e-tap-a-a="{{item.$original}}"><text>请选择</text><text class="icon-arrowDown"></text>
                    </view>
                </goods-item>
            </view>
            <view class="package-goods__item-footer">
                <view class="package-amount">组合价：</view>
                <at-button type="primary" __triggerObserer="{{ _triggerObserer }}" className="package-add-cart" size="small" bindonclick="funPrivateOcHbv" __fn_onClick="{{true}}">加入购物车</at-button>
            </view>
        </at-accordion>
    </block>
</block>