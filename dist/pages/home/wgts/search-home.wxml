<block>
    <block wx:if="{{!info}}">
        <view></view>
    </block>
    <view class="{{'wgt ' + (base.padded ? 'wgt__padded' : null)}}" wx:else>
        <view class="{{'search ' + (config.fixTop ? 'fixed' : null)}}">
            <view class="content-padded">
                <view class="search-box view-flex view-flex-middle view-flex-center" bindtap="searchTap" data-e-tap-so="this">
                    <icon class="search-icon" type="search" size="14" color="#999999"></icon>
                    <view>输入商品名称</view>
                </view>
            </view>
        </view>
    </view>
</block>