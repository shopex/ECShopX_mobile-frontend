<block>
    <block wx:if="{{!info}}">
        <view></view>
    </block>
    <view class="{{'wgt wgt-grid ' + (base.padded ? 'wgt__padded' : null)}}" wx:else>
        <view class="wgt-grid__header"><text class="wgt-grid__title">猜你喜欢</text>
        </view>
        <view class="wgt-body with-padding">
            <view class="grid-goods out-padding grid-goods__type-grid">
                <goods-item wx:key="$original.item_id" __triggerObserer="{{ _triggerObserer }}" info="{{item.$original}}" bindonclick="anonymousFunc0" wx:for="{{loopArray0}}" wx:for-item="item" wx:for-index="idx" __fn_onClick="{{true}}" data-e-onclick-so="this" data-e-onclick-a-a="{{idx}}"></goods-item>
            </view>
        </view>
    </view>
</block>