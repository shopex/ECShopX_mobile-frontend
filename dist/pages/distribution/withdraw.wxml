<block>
    <view class="page-distribution-withdraw">
        <view class="section withdraw">
            <view class="withdraw-title">可提现金额(元)：￥{{cashWithdrawalRebate / 100}}</view>
            <view class="withdraw-body">
                <at-input className="withdraw-body-input" __triggerObserer="{{ _triggerObserer }}" bindonchange="handleChange" type="number" placeholder="请输入提现金额" value="{{amount}}" __fn_onChange="{{true}}" data-e-onchange-so="this"></at-input>
                <view class="withdraw-body-btn" bindtap="handleWithdrawAll">全部提现</view>
            </view>
        </view>
        <view class="section list">
            <view class="list-item" style="position: relative;">
                <picker bindchange="handleChange" value="{{curIdx}}" range="{{payList}}">
                    <view class="pay-type-picker"></view>
                </picker>
                <view class="label">提现方式</view>
                <view class="list-item-txt content-right">{{payList[curIdx]}}</view>
                <view class="item-icon-go icon-arrowRight"></view>
            </view>
            <navigator url="/pages/distribution/withdraw_account_settings" class="list-item">
                <view class="label">提现账户</view>
                <view class="list-item-txt content-right">{{alipay_account ? alipay_account : '去设置'}}</view>
                <view class="item-icon-go icon-arrowRight"></view>
            </navigator>
        </view>
        <view class="content-padded">
            <button class="g-button {{isClick ? '_off' : ''}}" type="primary" bindtap="goWithdraw" disabled="{{!curIdx && amount > 800}}">提现</button>
        </view>
        <view class="g-ul">
            <view class="g-ul-li">每月只能提取2次，每次需大于等于1元</view>
            <view class="g-ul-li">金额低于{{limit_rebate}}元时不可提现</view>
            <view class="g-ul-li">预计三个工作日到账</view>
            <view class="g-ul-li">未实名认证的微信用户，将无法提现到收款账户</view>
        </view>
    </view>
</block>