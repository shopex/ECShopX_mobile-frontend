<block>
    <button class="{{'float-menu__item ' + (hide ? 'hidden' : '')}}" bindtap="funPrivatejfQCV" open-type="{{openType}}">
        <view class="{{iconPrefixClass + ' ' + iconPrefixClass + '-' + icon}}"></view>
        <slot></slot>
    </button>
</block>